<div class="jumbotron">
  <h1 class="display-4">Eat da Burgers!</h1>
</div>

{{!-- why doesn't my link work --}}
<img src="assets/img/burger_sandwich_PNG4132.png" alt="burger">


 <div class ="container-fluid"> 
</form>
<form class="create-form">
  <div class="form-row">
    <div class="col-5">
    <ul style ="list-style-type:none">
        <div
        {{#each burgers}}
        {{#unless devoured}}
        <b>{{id}}. {{burger_name}}</b><br>
        {{/unless}}
        {{/each}}
        </div>
    </ul>
    </div>
	<div class="col-2">

    {{burger_name}}
	<button class = "btn" "change-devour" data-id="{{id}}" data-newDevour="{{devour}}">
		{{#unless devour}}Devour{{/unless}}
	</button>
    </div>

 <div class="col-5">
    <ul style ="list-style-type:none">
        <div>
        {{#each burgers}}
        {{#if devoured}}
        <b>{{id}}. {{burger_name}}</b><br>
        {{/if}}
        {{/each}}
        </div>
    </ul>
</div>



<div class="form-group">
    <input class = "inputBox" type="text" id="ca" name="burger_name" size = "50" placeholder = "Yummy Burger">
    <button class = "btn" type="submit">Add Burger</button>
<br>
<label for="ca">add the burger you want and press "add burger"</label>
</form>
</div>